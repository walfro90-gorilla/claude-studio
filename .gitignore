node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Per-machine Claude Code permissions; the shared skills in .claude/skills are tracked.
.claude/settings.local.json

# Generated test footage, too big to track. CLAUDE.md has the command to rebuild it.
public/sample-bg.mp4

# Your own media, copied into public/ by `npm run short`. Only the sample
# fixtures (sample.mp3, captions.json) are tracked.
public/*.mkv
public/*.mov
public/*.webm
public/*.avi
public/*.m4v
public/*.mp4
